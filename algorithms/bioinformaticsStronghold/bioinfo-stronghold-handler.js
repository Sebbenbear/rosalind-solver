let DNA = require("/lib");

/*
 Handles all of the problems in the Bioinformatics stronghold branch of
 problems.
 Imports the modules for each of these problems.
*/

/*
 Locate the problem using the branch specified in the object.
 Then find the objects ID, and call the function on it - they should all be
 exported as modules.
*/

module.exports = {
  //everything that you need to return in here.

}
