/*
 Takes an input, finds out how best to format it, and returns the correct
 output.
*/
const fastaExpr = new RegExp("\\[.*?\\]", "g");  //change this

/* Helper methods go here - should be _private */

module.exports = {

  formatFASTA: (input) => {
    return "Need to enter something here";
  },

  formatSingleString: (input) => {
    return "Need to enter something here";
  }
}
